import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    paddingTop: 20,
    // marginBottom: 90,
    padding: 30
  },
  h1: {
    fontSize: 30,
    fontWeight: 'bold',
    color: '#8B4513'

  }
})
